function search() {
    const keyword = document.getElementById("searchInput").value;
    // Send an API request to your backend with the keyword
    // Update the "resultsContainer" with the search results
}
